tickTime=2000
dataDir=/var/lib/zookeeper/
clientPort={{ zookeeper['clientport'] }}
initLimit=5
syncLimit=2
{% for host in groups['zookeepers'] %}
server.{{ hostvars[host].zoo_id }}={{ host }}:{{ zookeeper['leader_port'] }}:{{ zookeeper['election_port'] }}
{% endfor %}

